__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store

# Checkpoints ship as release assets, not in git. reproduce_eval.sh creates this directory,
# downloads the weights into it, and copies the label map and temperature alongside them.
checkpoints/

# Outputs written by reproduce_eval.sh when it regenerates results for comparison.
reproduced/
